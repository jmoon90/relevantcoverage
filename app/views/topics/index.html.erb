<ul>
<% @topics.each do |topic| %>
 <li><%= simple_form_for @subscription do |f| %>
    <%= f.hidden_field :topic_id, :value => topic.id %>
    <%= topic.name %>
    <%= f.submit %>
  <% end %></li>
<% end %>
</ul>
